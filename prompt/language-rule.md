**Bulgu başlığı:** Türkçe (ör. `Servisler arasında tutarsız fallback`). İngilizce title yok.

**Label (kanonik, İngilizce, bold + colon):** `**File:**` / `**Issue:**` / `**Why it matters:**` / `**Suggested code:**`  
Türkçeleştirme yok (`Sorun` / `Neden önemli` yazma).

**Label altı:** Türkçe cümle; technical terms (endpoint, DTO, diff, null) İngilizce.

**Severity:** yalnızca **ÇIKTI FORMATI** map’i (Critical / Important / Minor).
